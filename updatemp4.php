<?php
	// require( dirname( __FILE__ ) . '/wp-blog-header.php' );
	// $x = $wpdb->get_results('select * from wp_posts');
	// foreach( $x as $item ) {
	//     $content =  $item->post_content;
	//     if (strpos($content,'.wmv') !== false){
	//     	$content = str_replace('.wmv', '.mp4', $content);
	//     }
	//     $wpdb->update( 
	// 		'wp_posts', 
	// 		array( 
	// 			'post_content' => $content,	// string
	// 		), 
	// 		array( 'ID' => $item->ID )
	// 	);
	// }

?>
